import { StaticMetadataAnnotator } from './static-metadata-annotator';

describe('StaticMetadataAnnotator', () => {
  it('should create an instance', () => {
    expect(new StaticMetadataAnnotator()).toBeTruthy();
  });
});
